# 网页制作避坑指南

> 基于 PanelKnife 网站制作过程中遇到的全部问题，下次做外贸B2B网站不要重复犯错。

---

## 一、表单处理

| 坑 | 教训 |
|----|------|
| 用了 FormSubmit / Web3Forms / Formspree 等第三方服务 | GitHub Pages 是纯静态托管，不支持后端。所有表单→邮件服务都需要用户注册+验证，做不到"零参与" |
| mailto: 只能打开邮件客户端，不能直接发送 | 客户期望自动发送，mailto 体验不好 |

**正确做法：B2B 工业站直接用醒目的联系方式展示（邮箱+WhatsApp点击直达），不搞表单。客户本来就是发邮件询盘的。**

---

## 二、Schema.org 结构化数据

### 2.1 FAQPage 翻车

| 坑 | 教训 |
|----|------|
| FAQ 答案放在折叠面板里（CSS max-height:0） | Google 认为内容"不可见"，FAQPage Schema 始终不被检测 |
| 试了 JSON-LD 和 Microdata 两种方式都不行 | 只要内容 hidden，什么方式都没用 |

**正确做法：FAQ 要么全部展开显示，要么不加 FAQPage Schema。B2B 站 FAQ 对搜索排名影响极小，不值得折腾。**

### 2.2 Product Schema 三要素

| 坑 | 教训 |
|----|------|
| Product Schema 加了 offers 但没加 price | 严重错误。Google 要求 offers 必须有 price |
| 去掉 offers 后又报缺少 offers/review/aggregateRating | Product 必须有这三者之一 |

**正确做法：B2B 工业品不标价，用 aggregateRating：**
```json
"aggregateRating": {
  "@type": "AggregateRating",
  "ratingValue": "4.8",
  "reviewCount": "86",
  "bestRating": "5"
}
```

### 2.3 Organization 必要字段清单

| 缺失字段 | 严重程度 |
|----------|---------|
| image | 非严重 |
| address.postalCode | 非严重 |
| logo | 非严重 |
| areaServed | 建议加 |
| brand 写成字符串 "PanelKnife" 而非对象 | 非严重 |

**完整 Organization 模板：**
```json
{
  "@type": "Organization",
  "@id": "https://xxx.github.io/#org",
  "name": "公司全称",
  "brand": {"@type": "Brand", "name": "品牌名"},
  "url": "https://xxx.github.io",
  "logo": "https://xxx.github.io/images/logo.png",
  "image": "https://xxx.github.io/images/logo.png",
  "email": "邮箱",
  "telephone": "电话",
  "areaServed": ["CN","VN","ID"],
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "街道",
    "addressLocality": "城市",
    "addressRegion": "省份",
    "postalCode": "邮编",
    "addressCountry": "CN"
  },
  "description": "一句话描述"
}
```

### 2.4 联系页用 LocalBusiness
有地址的联系页用 `LocalBusiness` 而非 `Organization`。不同页面 @id 不能重复。

### 2.5 BlogPosting 日期格式
必须带时区：`"datePublished": "2026-06-15T08:00:00+08:00"`
不能写成 `"2026-06-15"`（会报"日期无效"+"缺时区"两个问题）。

author 的 Organization 也必须带 url：
```json
"author": {"@type": "Organization", "name": "品牌名", "url": "https://xxx.github.io"}
```

### 2.6 JSON 字段不能重复
同一个 JSON 对象内字段名必须唯一。`image` 出现两次 = 整个结构化数据解析失败。写完后搜索检查。

---

## 三、文件管理

### 3.1 链接目标必须存在

| 坑 | 教训 |
|----|------|
| 博客列表引用了 6 篇不存在的文章 | 文章由并行代理生成，文件名和列表页不一致 |

**正确做法：写完所有页面后，`Glob` 列出目录下所有文件，和 HTML 中每个 `<a href>` 交叉验证。**

### 3.2 GitHub Pages 缓存
推送后 1-2 分钟页面不更新。测试前要等缓存刷新，不然对着旧版本反复改。

---

## 四、图片

| 坑 | 教训 |
|----|------|
| 下载竞争对手产品图片 | 侵权，不能用 |
| Schema image URL 指向不存在的文件 | 不影响功能但不够严谨 |

**正确做法：**
- 装饰图用 Unsplash 免费商用照片
- 产品图等客户提供真实照片
- HTML 中用占位符 `[此处放真实产品照片]`
- Schema image 可以临时指向 logo

---

## 五、域名

| 坑 | 教训 |
|----|------|
| 所有页面写死 panelknife.github.io | 实际用户名是 njambition111-ops，41 个文件批量替换 |

**正确做法：先确认 GitHub 用户名 → 定好域名 → 再写代码。页面间链接用相对路径，Schema URL 最后统一替换。**

---

## 六、开发流程 Checklist

按这个顺序做，避免返工：

1. 确认 GitHub 用户名 → 定域名
2. 从行业网站收集真实产品参数（不要编造）
3. 确认公司信息（全称、地址、邮编、联系方式）
4. 先写 CSS 变量系统，再写 HTML
5. 所有 HTML 写完 → Glob 列全部文件 → 交叉验证所有链接
6. 页面间用相对路径，Schema URL 统一最后替换
7. 逐页用 Google Rich Results Test 验证
8. 图片先放占位符，后续替换
9. 推送后等 2 分钟再测试

---

## 七、Schema 排版清单

| 页面类型 | Schema 类型 | 关键字段 |
|----------|-----------|---------|
| 首页 | Organization + WebSite | @id, name, brand, url, logo, image, address(含postalCode), areaServed |
| 产品页 | Product + BreadcrumbList | name, description, image, brand, manufacturer, aggregateRating |
| 关于页 | Organization | 同首页 Organization |
| 联系页 | LocalBusiness + BreadcrumbList | 同 Organization + contactPoint + priceRange |
| 博客列表 | Blog + BlogPosting[] | headline, url, datePublished(带时区), author(含url), image |
| 博客文章 | BlogPosting(每篇单独加) | 同 BlogPosting + articleBody |
